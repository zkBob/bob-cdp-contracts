{
  "name": "mellow-nft-stable",
  "license": "BSL-1.1",
  "version": "1.0.0",
  "files": [
    "src/**/*.sol"
  ],
  "repository": "https://github.com/mellow-finance/stable-nft",
  "devDependencies": {
    "prettier": "^2.3.1",
    "prettier-plugin-solidity": "^1.0.0-beta.13"
  },
  "scripts": {
    "prettier": "prettier --write **.sol",
    "prettier:check": "prettier --check **.sol",
    "test": "forge test -v",
    "test-v": "forge test -vvvv",
    "test:better": "forge test -v --etherscan-api-key $(grep ETHERSCAN_API_KEY .env | cut -d '=' -f2)",
    "test-v:better": "forge test -vvvv --etherscan-api-key $(grep ETHERSCAN_API_KEY .env | cut -d '=' -f2)",
    "size": "forge build --sizes --force --via-ir",
    "compile": "forge build",
    "deploy:mainnet": "forge script src/script/MainnetDeployment.s.sol:MainnetDeployment --rpc-url $(grep MAINNET_RPC .env | cut -d '=' -f2) --private-key $(grep PRIVATE_KEY .env | cut -d '=' -f2) --broadcast --verify --etherscan-api-key $(grep ETHERSCAN_API_KEY .env | cut -d '=' -f2) -vvvv",
    "deploy:rinkeby": "forge script src/script/RinkebyDeployment.s.sol:RinkebyDeployment --rpc-url $(grep RINKEBY_RPC .env | cut -d '=' -f2) --private-key $(grep PRIVATE_KEY .env | cut -d '=' -f2) --broadcast --verify --etherscan-api-key $(grep ETHERSCAN_API_KEY .env | cut -d '=' -f2) -vvvv",
    "deploy:polygon": "forge script src/script/PolygonDeployment.s.sol:RinkebyDeployment --rpc-url $(grep POLYGON_RPC .env | cut -d '=' -f2) --private-key $(grep PRIVATE_KEY .env | cut -d '=' -f2) --broadcast --verify --etherscan-api-key $(grep ETHERSCAN_API_KEY .env | cut -d '=' -f2) -vvvv"
  }
}
